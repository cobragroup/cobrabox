# Tests

Unit and integration tests for features and pipelines.

Feature test files follow the naming convention `test_feature_<feature_name>.py`.

Example contents:

- `test_feature_line_length.py`
- `test_feature_sliding_window.py`
